# trajokit
Sandboxed agent rollouts for RL post-training — tasks in, token trajectories with correct loss masks out. Async agent loop, Docker sandboxes, pluggable verifiers (unit tests or LLM judge). Trainer-agnostic.
